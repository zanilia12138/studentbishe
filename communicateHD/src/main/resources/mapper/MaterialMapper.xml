<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.health.communicate.mapper.MaterialMapper">
    <select id="selectMaterialsWithUserName" resultType="com.health.communicate.entity.Material">
        SELECT m.*, u.nickname AS user_name
        FROM material m
        LEFT JOIN user u ON m.user_id = u.id
        ORDER BY m.download_count DESC, m.create_time DESC
    </select>

    <select id="selectMaterialsWithUserNameByTitle" resultType="com.health.communicate.entity.Material">
        SELECT m.*, u.nickname AS user_name
        FROM material m
        LEFT JOIN user u ON m.user_id = u.id
        WHERE m.title LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY m.download_count DESC, m.create_time DESC
    </select>

    <update id="incrementDownloadCount">
        UPDATE material
        SET download_count = IFNULL(download_count, 0) + 1
        WHERE id = #{id}
    </update>
</mapper>
